<%= form_for event, :remote => true, :index => event.id, :html => { :class => 'item-form'} do |f| %>
  <%- cancel = link_to("Cancel", show_row_event_path(event), :remote => true) -%> <%# TODO RAILS3: attempt to replace link_to_remote :update => dom_id(event), :method => :get %>
  <%= render :partial => 'form_fields', :locals => { :f => f, :event => event, :button_text => 'Update', :cancel => cancel } %>
<% end %>