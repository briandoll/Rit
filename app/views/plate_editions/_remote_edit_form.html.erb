<% remote_form_for plate_edition, :index => plate_edition.id, :html => { :class => 'item-form'} do |f| %>
  <%- cancel = link_to_remote("Cancel", :url => plate_edition_path(plate_edition), :update => dom_id(plate_edition), :method => :get) -%>
  <%= render :partial => 'form_fields', :locals => { :f => f, :plate_edition => plate_edition, :button_text => 'Update', :cancel => cancel } %>
<% end %>